// 仅 debug 构建：asInvoker manifest（宿主开发机渲染冒烟用）。
// release 用 icemelt.rc（requireAdministrator）；选择逻辑在 xmake.lua 的
// icemelt-gui 目标里，按构建模式二选一。
#include <winresrc.h>

CREATEPROCESS_MANIFEST_RESOURCE_ID RT_MANIFEST "icemelt-asinvoker.manifest"
